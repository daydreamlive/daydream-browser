{
  "name": "@examples/with-vanilla",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build:client && node server.js",
    "build": "npm run build:client",
    "build:client": "esbuild src/main.js --bundle --outfile=public/main.js --format=esm"
  },
  "dependencies": {
    "@daydreamlive/browser": "^0.3.2",
    "@daydreamlive/sdk": "^0.1.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
